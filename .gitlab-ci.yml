image: node:10.6.0

stages:
  - build
  - test

cache:
  paths:
    - node_modules/

build site:
  stage: build
  script:
    - npm install --progress=false
    - npm run build
    - scp -P22 -r dist/* root@111.230.224.164:root/test
  artifacts:
    paths:
      - public
